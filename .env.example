# Example environment variables for HMIP.
# See 09_Deployment_Guide.md section 5. Copy to .env for local runs
# (never commit a real .env — see .dockerignore / .gitignore).

# Runtime environment name (development | staging | production).
HMIP_ENV=development

# Overrides for core/models.py::BootstrapReport's runtime_version /
# kernel_version fields. Optional — falls back to placeholder defaults
# in shared/constants.py if unset (see SPRINT_3_STATUS.md's open
# question about what these should really be tied to).
HMIP_RUNTIME_VERSION=
HMIP_KERNEL_VERSION=

# Log level for core/observability.py's structlog configuration.
HMIP_LOG_LEVEL=INFO

# Secret provider selector. No provider is actually implemented yet —
# core/config.py's secret resolution is a documented pass-through stub
# (see SPRINT_1_STATUS.md). Reserved for when one exists.
HMIP_SECRET_PROVIDER=

# Base directory for config/*.yaml lookups. Defaults to ./config if
# unset.
HMIP_CONFIG_PATH=

# Where domain data pipelines read/write (data/raw, data/normalized,
# etc.). Not currently read by any code — no data pipeline exists yet
# that would use it (listed here because 09_Deployment_Guide.md
# section 5 names it as a standard env var).
HMIP_DATA_DIR=

# Where platform_/bootstrap.py writes BootstrapReport JSON files.
# Defaults to ./reports if unset.
HMIP_REPORT_DIR=

# Where platform_/readiness.py writes/reads its readiness marker file.
# Defaults to a path under the OS temp directory if unset.
HMIP_READY_FILE=

# ---- Price Intelligence: collector chain (4-tier) ----------------------
# Thứ tự fallback: Firecrawl → ScraperAPI → ZenRows → Jina.
# Khi Firecrawl hết credit (402), circuit breaker tự skip sang tier dưới.
FIRECRAWL_API_KEY=          # Tier 1 (đáng tin, tốn credit). Hết credit 14/09 → refresh.
SCRAPERAPI_KEY=             # Tier 2 (free 5000/tháng, render JS). Fallback chính khi Firecrawl 402.
ZENROWS_KEY=                # Tier 3 (free 1000/tháng, render JS). Fallback tier 2.
# Jina Reader (tier 4) free, không cần key.

# ---- Multi-channel: kênh thu thập giá ---------------------------------
# Danh sách kênh bật, comma-separated (mặc định chỉ tiki).
# Mỗi kênh lưu observation riêng (channel_id) → so sánh giá cross-channel.
# Tiki   = API JSON (/api/v2/products, chính xác).
# Shopee = HTML search (bot protection → cần ScraperAPI/ZenRows render JS).
# Lazada = HTML search (Akamai bot protection, khó hơn Shopee).
HMIP_CHANNELS=tiki         # ví dụ: tiki,shopee,lazada

# ---- Notify: alert giá thay đổi sang Telegram + Discord ---------------
# Chỉ gửi khi giá THAY ĐỔI (incremental), không gửi khi giá không đổi.
# Chống spam (3 lớp, áp trong notify_alert):
#   HMIP_NOTIFY_MIN_PCT   — % thay đổi tối thiểu để notify (mặc định 5;
#                           bỏ noise dao động 1-5%).
#   HMIP_NOTIFY_SKU_COOLDOWN_MIN — phút chờ giữa 2 notify cho cùng SP (bất kể
#                           kênh nào) (mặc định 30; 0 = tắt). Chống spam khi
#                           multi-channel (Tiki+Shopee+Lazada) cùng đổi giá
#                           1 SP → chỉ kênh đầu notify.
#   HMIP_NOTIFY_COOLDOWN_MIN — phút chờ giữa 2 notify cho cùng (sku,channel,
#                           region) (mặc định 360 = 6h; 0 = tắt cooldown).
HMIP_NOTIFY_MIN_PCT=5
HMIP_NOTIFY_SKU_COOLDOWN_MIN=30
HMIP_NOTIFY_COOLDOWN_MIN=360
TELEGRAM_HMIP_MARKET_BOT=   # 🔒 bot token (từ @BotFather)
TELEGRAM_CHAT_ID=8891619372 # chat_id nhận alert
DISCORD_BOT_TOKEN=          # 🔒 bot token (từ Discord Developer Portal)
DISCORD_HOME_CHANNEL=       # channel ID (để trống nếu dùng DM)
DISCORD_DM_USER_ID=1519733836819202078  # user ID nhận alert qua DM (bot tự tạo DM)

# ---- Workflow PRC-001 notify (/api/run, scheduler scan_once) ----------
# Hệ thống notify thứ hai (riêng với PI incremental). Cùng chat_id/user_id.
# Chống spam (3 lớp, áp trong notify_all → rate_limit.allow):
#   HMIP_SCAN_NOTIFY_MIN_PCT      — % delta tối thiểu để gửi (mặc định 10;
#                                   chỉ ESCALATE đi qua, ALERT 5-10% bị skip).
#   HMIP_SCAN_NOTIFY_COOLDOWN_MIN — phút chờ giữa 2 notify cho cùng SP (mặc định
#                                   360=6h; 0=tắt. Giá đổi ≥5% thì vẫn báo).
HMIP_SCAN_NOTIFY_MIN_PCT=10
HMIP_SCAN_NOTIFY_COOLDOWN_MIN=360
HMIP_TELEGRAM_BOT_TOKEN=    # 🔒 bot token (có thể cùng bot với TELEGRAM_HMIP_MARKET_BOT)
HMIP_TELEGRAM_CHAT_ID=8891619372
